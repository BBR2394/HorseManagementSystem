CREATE TABLE coat ( ID SERIAL PRIMARY KEY, name VARCHAR(56));
CREATE TABLE horses ( horse_id SERIAL PRIMARY KEY, name VARCHAR(255) NOT NULL , size INTEGER, coat NUMERIC, father NUMERIC, mother NUMERIC, sex NUMERIC, current_owner NUMERIC, created_on TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP)